const deviceName = 'ESP32';
const serviceUUID = 'your-service-uuid'; // Substitua pelo UUID do seu serviço
const characteristicUUID = 'your-characteristic-uuid'; // Substitua pelo UUID da sua característica

export { deviceName, serviceUUID, characteristicUUID };
